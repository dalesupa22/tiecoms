<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.TieComs" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/cream</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Splash del sistema (Android 12+ y compat): símbolo de Chaggu sin rayitas sobre tinta, sin círculo de color.
         splash_icon es el lienzo de 288 dp con la capa (176 dp) centrada: las burbujas caben en el círculo de 192 dp.
         Da paso al splash animado, que empieza con el mismo símbolo en el mismo sitio. -->
    <style name="Theme.TieComs.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.TieComs</item>
    </style>
</resources>
