# En tiecoms.com y www: los links de la app van directo a app.tiecoms.com
# (si la app no está instalada, se abre la web en la misma ruta).
location ~ ^/(c|w|invite)/ { return 302 https://app.tiecoms.com$request_uri; }
location = /signup { return 302 https://app.tiecoms.com$request_uri; }
